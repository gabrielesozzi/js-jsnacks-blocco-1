// L’utente inserisce due numeri in successione, con due
// prompt. Il software stampa il maggiore.

// Chiedo numeri ad utente

var number1 = parseInt (prompt("Inserisci il primo numero"));
var number2 = parseInt (prompt("Inserisci il secondo numero"));

// Inserisco consizione

if (number1 > number2) {
  console.log(number1);
}else {
  console.log(number2);
}
